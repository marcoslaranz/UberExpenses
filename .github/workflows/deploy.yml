name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4   # ✅ latest

      - name: Setup .NET
        uses: actions/setup-dotnet@v4   # ✅ latest version
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build and publish
        run: dotnet publish -c Release -o release --nologo

      - name: Change base href
        run: sed -i 's|<base href="/" />|<base href="/UberExpenses/" />|g' release/wwwroot/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2   # ✅ correct action for Pages
        with:
          path: ./release/wwwroot

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4   # ✅ latest